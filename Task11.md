# Практическое задание №1
`exit={{ y: -100% }}`
# Практическое задание №2
```jsx
whileInView={{ opacity: 1 }}
viewport={{ once: true }}
```
# Контрольные вопросы
1. Потому что элемент мгновенно пропадает из `React-DOM` 
2. `drag={{ x }}`
3. Анимирует изменения элементов вместо их мгновенного перемещения
4. `shileInView` позволяет более гибко реагировать на появление в `viewPort`
5. Для мягкого ограничения при перемещении